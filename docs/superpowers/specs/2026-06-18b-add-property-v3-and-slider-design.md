# Форма добавления v3 + бегунок адреса — design (2026-06-18b)

**Контекст.** Второй батч правок создателя по форме добавления (часть уточняет FC-4). Зафиксировано
сразу (урок: [[feedback-record-requests-immediately]]). Реализация — Subagent-Driven. Слой 3/4/
комментарии — на паузе.

---

## Запросы (вербатим, пронумеровано как у создателя)

### Форма добавления

1. Ввод адреса Damac Hills.
2. 🔴🐛 Уточнение адреса: «Golf Vista» находит, «Vista» — нет. → `search_locations` глобальный +
   `LIMIT 20` выбивает подстроку из топа; клиент фильтрует по комьюнити → пусто. **Фикс = скоуп
   поиска к комьюнити в RPC = миграция (ГЕЙТ).**
3. «Что видят соседи» → переименовать в **«Что видят коллеги»**.
4. maid/hotel/vastu: три строки, чекбокс рядом, по левому краю; третий пункт — **«Vastu
   Compliant»** (переименовать с «Vastu»). ⚠️ точная раскладка — OPEN Q1.
5. **Developer-автокомплит**, если по адресу нет данных в `location_developers`. Источник —
   таблица `developers` (поиск по имени). ⚠️ условие показа — OPEN Q3.
6. **Главное фото + порядок**: юзер может сделать любое фото главным (перенести на 1-е место) и
   менять порядок фото; порядок сохраняется (`order_index`) и в нём же показывается. Сейчас не нравится.
7. Слияние шага 2 в шаг 1 (FC-4 сделано), но **Sale/Rent поднять в самый верх — первой строкой**
   (выше категории).
8. Последний шаг разбить на два: **Описание = шаг 7, Фото = шаг 8** (отдельно).
9. **Шаг 8 «Фото»** — красивая строчная галерея (исп. имеющийся стек: Swiper.js есть; reorder —
   Angular CDK DragDrop). Ниже — строка **«Floor Plan»**: до 4 фото планировки, тоже со сменой
   порядка 1–4 (`photo_type='floor_plan'`, уже есть в схеме). ⚠️ судьба `layout_id`-чипов — OPEN Q2.
10. **Спальни и санузлы — обязательные.** Все обязательные поля пометить **серой звёздочкой** `*`.

### Бегунок приватности адреса (шаг «Адрес», при добавлении и редактировании)

S1. «Что видят коллеги» — **центрировать**.
S2. Бегунок **центрировать** и сделать **шириной всего адреса**: начало — с половины слова «Dubai»,
конец — на половине последнего слова (leaf).
S3. На бегунке — **круглые точки**, каждая центрирована относительно своего level-слова адреса.

---

## РЕШЕНИЯ (закрыто 2026-06-18b)

- **Q1 (#4) → раскладка B:** метки слева, **чекбоксы выровнены в колонку** (друг под другом),
  строки по левому краю. Переименовать «Vastu» → **«Vastu Compliant»**.
- **Q2 (#9/#10) → layout_id остаётся в «Параметры»**, **над полем площади (BUA)**, и доступен
  **только для residential `house`** (убрать `layout:true` у `villa_commercial`). Floor Plan-фото
  (#10) — отдельно на шаге 8.
- **Q3 (#5) → показывать Developer-поле ТОЛЬКО когда `buildingInfo()===null`** (нет строки
  `location_developers` по адресу); автокомплит из таблицы `developers` по имени.

### Лента (новый пункт, «добавь в таску»)

- **F-center:** в строке ленты центрировать колонки **Тип · Beds · Площадь · Цена · иконки · Агент**
  (сейчас по левому краю). Чисто CSS (`feed`/`property-card` компонент). Отдельно от формы.

## Гейты / зависимости

- **#2 search_locations** — миграция (скоуп к комьюнити). Применяет создатель в Studio.
- **#5 developers** — проверить, читается ли таблица `developers` под anon (RLS). Если нет — RPC/политика (миграция).
- **#7/#10 photo reorder + Floor Plan** — без DB-миграции (`order_index`+`photo_type` есть); нужен
  CDK DragDrop + правки `PropertyPhotoService` (загрузка floor_plan, сохранение порядка).

## Целевая структура шагов (после батча, 8 шагов)

1. Категория (**Sale/Rent первой строкой**, затем категория→тип→подтип) · 2. Адрес (+ бегунок S1-S3) ·
2. Параметры (beds/baths обязательные\*, чекбоксы #4; layout_id — по Q2) · 4. Цена · 5. Состояние ·
3. Листинг · 7. **Описание** · 8. **Фото** (галерея reorder + Floor Plan).

## Декомпозиция (waves, по очереди; гейты — отдельно)

- **WP-1 (без миграций, шаблон/CSS):** #3 rename, #4 чекбоксы (по Q1), #8 Sale/Rent наверх,
  #11 обязательные+звёздочки, бегунок S1-S3.
- **WP-2 (реструктуризация шагов):** #9 разбить Описание(7)/Фото(8), `layout_id` по Q2.
- **WP-3 (фото):** #7/#10 reorder-галерея + Floor Plan (CDK DragDrop, PropertyPhotoService).
- **WP-4 (developer):** #5 автокомплит (после проверки RLS developers).
- **WP-5 (ГЕЙТ):** #2 миграция search_locations (скоуп к комьюнити).

## Критерии успеха

Каждая правка проверяема: rename виден; три чекбокса по согласованной раскладке + «Vastu Compliant»;
Sale/Rent первой строкой; beds/baths блокируют «Далее» если пусто, звёздочки у всех обязательных;
шаг 7 Описание, шаг 8 Фото; фото перетаскиваются и порядок сохраняется (1-е = главное); Floor Plan до 4;
developer-автокомплит при пустом location_developers; бегунок центрирован, на ширину адреса, с точками;
«Vista» находит «Golf Vista». `checkFile` зелёный, юнит-тесты на навигацию/валидацию/порядок, полный сюит зелёный.
