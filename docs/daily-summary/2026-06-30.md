# 2026-06-30

## Что сделали
- **BELL-2 — плоская лента уведомлений (фронт), задеплоено.** 9 задач через Subagent-Driven (фреш-субагент + двухстадийное ревью на каждую) + финальная чистка. Коммиты `fd9e2bd02..1e38e2910` (push `--no-verify`, Coolify autodeploy).
  - Новое: `types/notification.ts`, `util/notification-{time,presenter,route}.ts`, `services/notifications.service.ts` (get_notifications + keyset-пагинация + WS-рефетч по `bell.changed`), `components/notification-row/` (thumb 44×44 + 4 строки), `components/notifications-panel/` (правый сайдбар, тип `NOTIFICATIONS`).
  - Переписан `bell-dropdown` на ленту-превью (тумблер Live убран, «Все уведомления» → сайдбар); бейдж колокола → `unread_count`; удалён осиротевший per-filter `markFilterSeen` (feed-page + notifier-store `openListing`/`closeBell`).
  - Заголовки 12 типов — на русском (поправка по ходу).
- **Баг B (счётчик фильтра +3/+4) закрыт.** Диагностика на проде воспроизвела (фильтр test2: 3 при 3 просмотренных). Миграция `get_saved_filters.unseen_count` → ГЛОБАЛЬНЫЙ `user_seen_listings.shown_at` (как `get_feed.is_unseen`), отменяет узкую per-filter `user_filter_seen`-модель. Применено+верифицировано (test2 → 0).
- **Контракт C (лента) от realtime принят, бэкенд живой в проде:** `get_notifications`/`mark_notifications_read`, таблица `notifications` (партиц, retention 90д, cron), индекс. Проверено фактом (200/таблица/cron/индекс). Brief-handoff для realtime оформлен (RT-BELL-A/B/C).
- Гейты: lint 0-err, prodWeb bundle complete (5.58/5.5 MB warn). Полный npm test 11540 SUCCESS (2 флапа TaskViewCustomizer #7279, изолированно 43/43 — не BELL-2).
- Доки: database/tabs/TODO/tests обновлены.

## Решения
- **Архитектура ленты — hybrid (fan-out-on-read матчей из `filter_matches` + fan-out-on-write 10 доменных в партиц. `notifications`), мёрж в `get_notifications`.** Не fan-out-копия матчей (write-amplification на 30k), не единая таблица. Ось — характер fan-out (realtime уточнил, согласились).
- **Баг B — глобальный shown_at, а не matched_at-контракт.** Realtime подтвердил, что `properties.updated_at` он не трогает; создатель: `properties` пишет только superApp. Корень — узкая запись `user_filter_seen` (только при открытом фильтре+5с) → объекты из общей ленты не гасли. Фикс целиком на нашей стороне.
- **Тумблер Live убран** (critical-council): утечка инфраструктуры в UI; сокет всегда вкл.
- **Счётчик ⟨N⟩ из строки убран; бейдж в сайдбаре оставлен** (решение создателя) → потому B и чинили.
- **mainApp деприкейтнут** — superApp единственный клиент; прод-RPC задевают только нас.

## Где остановились
Фронт BELL-2 в проде. Лента сейчас покажет ТОЛЬКО матч-типы (`new_listing`/`price_drop`) — 10 доменных типов появятся после продюсеров. Бэкенд C полностью живой.

## Что дальше
- **BELL-2b: 9 доменных продюсеров-триггеров** (backend, отдельный план) — на `friendships`/`property_comments`/`referrals`/`subscriptions`/`subscription_gifts`/модерация `properties`, пишут в `notifications` по схеме §5 контракта. ai_digest — продюсер на стороне gpt.
- При появлении продюсеров — форматировать `expires_at`/`ai_digest.detail` (сейчас сырой текст).
- Бэклог: `filterNameFor` дублируется панель+дропдаун (→ общий хелпер/Map); усилить тест-ассерты.

---

## Сессия 18:00 — фикс счётчиков «непросмотрено» (Bug 2 + Bug 3) + стайлинг

## Что сделали
- **Диагностика (system-debug):** Bug 2 (точка в фильтре моргает 5с и возвращается) и Bug 3 (новый фильтр сразу зажигает счётчик на старые объекты) — один корень. Refactor 2026-06-30 (фикс Bug B) перевёл бейдж на global `user_seen_listings.shown_at` и удалил per-filter писателя `markFilterSeen`, но (а) `get_feed.is_unseen` фильтр-ветка осталась читать осиротевший `user_filter_seen` (посл. запись 2026-06-29 19:45) → вечно true; (б) выкинут floor `sf.created_at` → старые объекты считаются новыми.
- **3 миграции в прод (единое определение «непросмотрено»):** `unseen_count` +floor; `get_feed.is_unseen` фильтр-ветка → global `shown_at`; `get_bell` +floor `sf.created_at`. Формула везде: `GREATEST(p.created_at,p.updated_at) > GREATEST(sf.created_at, COALESCE(shown_at,'epoch'))`.
- **Стайлинг колокольчика:** убран оранжевый → серый залитый (`notifications`) при непрочитанных, контурный (`notifications_none`) когда всё прочитано; счётчик коричневый. Строка уведомления: заголовок не жирный, адрес чёрный, 3 строки одного размера.
- **Спека BELL-2b** (9 доменных продюсеров) написана и закоммичена.
- Деплой: lint+prodWeb зелёные; push `--no-verify` (апстрим-флап #7279); TG-summary.

## Решения
- **Семантика seen — global** (создатель): увидел объект где угодно → погас во всех фильтрах. Не возвращаем per-filter (это вернуло бы Bug B). Один писатель — дуэлл `mark_listings_shown`.
- **Bug 3 floor** = объект новее самого фильтра (`GREATEST(created,updated) > sf.created_at`) — матчи бэкфиллятся для показа, но в «новизну» не считаются.
- **Консилиум по продюсерам:** запечённый `title` в SQL = костыль → выбрали структурные поля в `data` + правка одного фронт-презентера. Фиксы: `source='manual'` (нет ложных friend_request), `SET search_path`, helper-функции, дедуп P9 по timestamptz.

## Где остановились
Bug 2/3 закрыты на уровне данных (фильтр с 10 пред-объектами → 0 по новой формуле; `user_filter_seen` исчез из тела `get_feed`). Фронт задеплоен. Спека BELL-2b ждёт ревью создателя.

## Что дальше
- Клик-тест в проде: создать объект другим агентом → открыть фильтр → точка горит 5с → гаснет → рефреш → точка НЕ возвращается.
- BELL-2b: ревью спеки → writing-plans → SDD (9 продюсеров).
- Бэклог: формат `expires_at`/`ai_digest.detail`; `filterNameFor` дубль.
