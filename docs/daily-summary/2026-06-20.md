# 2026-06-20

## Что сделали

- **Голосовой ввод чата — редизайн «ChatGPT-стиль» (S-5 follow-up).** При нажатии 🎤
  весь композер превращается в строку записи: метка **«Слушаю…»**, **живая звуковая
  дорожка** (Web Audio `AnalyserNode` → `<canvas>`, столбики реагируют на громкость
  голоса, тише → бледнее/«гаснут»), таймер `m:ss`, кнопки **✕ отмена** / **✓ готово**
  (микрофон скрыт, кнопка отправки → галочка). ✓ → **«Расшифровка…»** со spinner в той
  же строке → Whisper → текст в поле + фокус. ✕ — отбросить без расшифровки.
- **Фикс «скачущего» композера на планшете.** Найдена причина: оболочка
  (`.route-wrapper > router-outlet + *`) навешивает на хост страницы второй слой
  скролла (`overflow-y:auto` + `touch-action:pan-y`); после отправки он прокручивался и
  плавающий композер прыгал вверх. Хост заперт (`overflow:hidden`), скроллится только
  лента (`min-height:0` + `overscroll-behavior:contain`), поле схлопывается
  детерминированно (не по устаревшему `scrollHeight`).
- Деплой `c7b93d923`: 25 тестов чата ✅, prodWeb ✅, pre-push прогнал полный сьют
  (11186 + 11172 в двух TZ) ✅, TG-summary отправлен.

## Решения

- **Дорожка реагирует на голос (Web Audio), не декоративная анимация** — пользователь
  явно хотел «видеть, что идёт по голосу раскадровка». Canvas (не DOM-бары) → ноль
  нагрузки на Angular CD при 60fps.
- **Добавлены ✕ (отмена) и таймер** сверх буквального запроса — без них «Слушаю…»
  выглядело бы оборванным; обе вещи есть в ChatGPT. Оставлены с пометкой «уберу, если не
  нужно».
- **Причину планшетного бага вычислил по коду**, не на устройстве — фикс по учебнику
  тач-скролла; финальная проверка — за пользователем на реальном планшете.
- Гоча зафиксирована в память: `checkFile` = prettier→eslint, `no-mixed-operators` хочет
  скобки, prettier их срезает → дробить арифметику на промежуточные переменные.

## Где остановились

Задеплоено и live (Coolify autodeploy с пуша). Документация обновлена (tabs.md, TODO.md
S-5). Ждём проверку на планшете.

## Что дальше

- Пользователь тестит на планшете: ушла ли «болтанка» композера после отправки.
- Если ✕/таймер не нужны — убрать.
- UX-3 (реальное удаление переписки, UAE PDPL) — остаётся в беклоге перед продом.
