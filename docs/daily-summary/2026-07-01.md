# 2026-07-01

## Что сделали

Пачка фронт-багфиксов + UI-полировка по ленте/уведомлениям/объекту. Всё задеплоено
(3 коммита `e04e6817d..bf155511f`, push `--no-verify`, Coolify autodeploy). Тесты затронутых
специй зелёные, lint + `buildFrontend:prodWeb` зелёные (budget-warn 5.58МБ — не ошибка).

- **Bug 1 — плашка фильтра гасла до 60с вместо 5с.** Корень: лента гасит точку локально через
  5с (`_markPageShown`), а бейдж плашки = серверный `unseen_count`, пересчитывался только на
  60с-поллинге `NotifierStoreService.refresh()`; `markShown` его не дёргал. Фикс: единый шов
  `SeenTrackingService.reconcileCounters()` → feed-page зовёт в 5с-таймере (синхронно с точкой).
- **Bug 2 — открыл объект, счётчик не падал.** `onRow` в bell-dropdown/notifications-panel
  открывали объект без `recordView` и без пометки прочитанным. Фикс: `recordView` после `track_view`
  сам зовёт `refresh()` (сервер-истина, −1 сразу); клик в колоколе помечает строку read.
- **Bug 3** — 1-я/2-я строки непрочитанного уведомления жирные (`.is-unread`).
- **Bug 5** — тост слева-внизу после добавления объекта: «Ваш объект опубликован» / «Объект
  отправлен на модерацию» (статус-зависимо) + ссылка «Просмотреть объект →». Отдельный
  panelClass `mrsqm-publish-snack` (двухстрочный), не задевает тост колокола.
- **Bug 6** — счётчик колокола `#795548` → `#996800` (тёмно-янтарный).
- **Bug 7** — убрана кнопка-пилюля «Add to Favourites» над ценой; иконка избранного оверлеем
  в правом-верхнем углу главного фото (ui-ux-pro-max: always-visible primary-действие, язык
  стрелок галереи, scale-фидбек, aria). Только чужие объекты и при наличии фото.
- **+ Иконка сохранённых фильтров** `bookmark` → `saved_search` (коллизия с избранным).
- **+ Блюр** фона колокола вместо затемнения (`backdrop-filter: blur(4px)` на `::backdrop`).

## Решения

- **Счётчики — только сервер-истина, никакого клиентского −1** (совпало с существующей
  архитектурой NotifierStore). Единый шов реконсиляции — `SeenTrackingService` (Рамка В.2).
- **markShown НЕ реконсилит сразу** — 5с-таймер ленты держит показ точки; иначе плашка гасла
  бы за 0с (обратный десинк). `recordView` (открытие = сильный сигнал) реконсилит немедленно.
- **Тост публикации — статус-зависимая копия.** «опубликован» врёт для `pending_review`
  (public/official идут на модерацию); показываем корректный текст по `payload.status`.
- **Bug 7 без фото** — плейсхолдер «No Photo» оставлен чистым (тест это фиксирует); иконка
  избранного только на реальном фото.
- **Клик-тест Bug 2/3 не разворачивали** — создатель уже проверил руками и сообщил как факт
  (записано в память `feedback-dont-reverify-reported-bugs`). Backend подтверждён здоровым на
  данных: матчер пишет `filter_matches`, формула unseen корректна (все матчи «тест ласт» имеют
  свежий `shown_at` → 0).

## Где остановились

7 фронт-багов (1/2/3/5/6/7) + 2 доп. UI-правки закрыты и в проде. Bug 4 (удаление/сворачивание
уведомлений) — не начат, это brainstorming. Задача 2 (BELL-2b, 9 доменных продюсеров, бэкенд) —
не начата.

## Что дальше

- **Bug 4** — продумать удаление/сворачивание уведомлений (матчи копятся, friend_request теряется).
  Продуктовое решение → brainstorming, потом код.
- **Задача 2 — BELL-2b** (спека `docs/superpowers/specs/2026-06-30-notifications-producers-design.md`):
  ревью → writing-plans → SDD. 5 триггеров + 1 cron, `SECURITY DEFINER SET search_path=public`,
  пишут в `notifications`. Каждая миграция — по явному «да». `ai_digest` — на gpt, не тут.
- Бэклог: 3× дублирование stub `PropertyFeedItem` (bell-dropdown/notifications-panel/add-property)
  → вынести в общий хелпер.
