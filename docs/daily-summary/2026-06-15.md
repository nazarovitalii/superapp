# 2026-06-15

## Что сделали

- **Форма добавления объекта v2** — переписана на 8 шагов: Категория → Сделка → Адрес → Параметры → Цена → Состояние → Листинг → Описание.
- **Каскад адреса до leaf**: `search_locations` (mode=search) → выбор → mode=info отдаёт children; поиск если children>10, чипы если ≤10; leaf = children пуст. После leaf — Building info из `location_developers` (год, этажи, юниты) и планировки из `community_layouts`.
- **Поля по типам объекта** — новый модуль `property-type-fields.ts`: набор полей шага «Параметры» зависит от unit_type (сверено с живой БД через REST get_filter_options). Чекбоксы `is_maid`/`is_hotel_pool`, BUA+Plot, floor_number/level, floors_in_unit (G+…), layout, мультиселекты views/positions/amenities, furnished.
- **Состояние**: off-plan → completion_year/q + developer_id из leaf; ready → occupancy; occupied → lease_until (месяц+год). **Листинг**: Title Deed №/год, plot/municipality number.
- `PropertyInsert` расширен всеми новыми полями; сервис получил `locationInfo`/`getBuildingInfo`/`getCommunityLayouts`; спека обновлена. Сборка + 10945 тестов зелёные. Задеплоено.

## Решения

- Хранение фото — **Supabase Storage** + нарезка в браузере (зафиксировано ранее, P-5b).
- **Расхождение таксономии БД vs CSV**: в живой БД `hotel_apartment` — коммерческий unit_type (в CSV был подтипом Apartment); «Residential Land» в БД = `land`. Подтипы Apartment уже без Hotel Apartment. Форма построена на реальной таксономии БД; чекбокс hotelPool показывается для apartment+hotel_apartment.
- Прямой psql к Supabase закрыт (порт 5432) — таксономию читал через REST RPC с anon-ключом.

## Где остановились

- Форма v2 на проде (после сборки образа — проверить sapp.mrsqm.com).
- Слайдер приватности адреса (F-12b) и Form A (F-12d) — отложены: нужны миграция БД (колонка типа `public_location_id` + правка get_feed/get_property) и Storage-bucket.

## Что дальше

- Согласовать миграцию под слайдер F-12b → реализовать интерактивный бегунок.
- Баги F-2 (toggle sidebar) и F-3 (сортировка по новизне в обратном порядке).
- Комментарии (F-13\*), профиль (F-17), фильтры по типам (F-5/F-16f).

---

## Сессия 3 — слайдер приватности + фото

### Что сделали

- **Слайдер приватности адреса (F-12b)**: `revealIndex` signal, clamp до `communityIndex`, `publicLocationId` computed = `addrPath[revealIndex].id`; тики подсвечиваются; payload пишет `public_location_id`.
- **Фото объекта (P-5b)**: `PropertyPhotoService` — нарезка в браузере (canvas→WebP, full 1600px/thumb 400px), загрузка в bucket `property_photos`, INSERT в `property_photos`; превью в форме с кнопкой удаления; первое фото — бейдж «Главное».
- **Типы БД**: `PropertyPhotoInsert`, `LocationInfo`/`BuildingInfo`/`CommunityLayout`, `public_location_id` в `PropertyInsert`.
- **Миграции** применены в Studio:
  - `2026-06-15-add-public-location-id.sql` — колонка `properties.public_location_id`
  - `2026-06-15-property-photos.sql` — bucket + 3 Storage RLS + 1 table RLS + CHECK
- Сборка зелёная, задеплоено. Миграции перенесены в `docs/migrations/applied/`.

### Решения

- Bucket Storage через anon-ключ не виден через `/bucket/:id` (требует service role) — проверяли через `/object/list/` (вернул `[]` — ОК).

### Где остановились

- Форма добавления полностью готова; фото + приватность адреса работают в коде.
- Показ фото в ленте/карточке — следующий шаг (обогащение без правки get_feed).

### Что дальше

- Отображение фото в `feed-page` и `property-card` (first photo из property_photos).
- Баги F-2 (toggle sidebar), F-3 (сортировка по новизне).
- Комментарии F-13, профиль F-17, фильтры по типам F-5/F-16f.

---

## Сессия 4 — фиксы формы добавления

### Что сделали

- **Каскад адреса по всем уровням**: внутри комьюнити «Уточните адрес» теперь глобальный поиск по sub_community/cluster/building — раньше показывались только прямые дети. Новый computed `useDescendantSearch` + метод `onChildSearchInput` + клиентский фильтр по `community_name`.
- **Фикс краша публикации** (`TypeError: F.replace is not a function`): `<input type="number">` + ngModel клали number в string-сигнал; `num()` падал на `.replace`. Все числовые поля переведены на `inputmode="numeric"`, `num()` и lease-расчёт приводят к `String`.
- **Убран номер этажа**: `floor_number`/`floorNumber` удалён из интерфейса `TypeFields`, конфига по типам и формы; уровень этажа (Low/Mid/High) оставлен.
- **Pocket без документов**: Title Deed/Plot/Municipality + Form A скрыты и не уходят в payload если `listingType !== 'official'`.
- `docs/tabs.md` обновлён.

### Где остановились

- Форма добавления объектов полностью рабочая (каскад, кэш, публикация).
- Фото в ленте/карточке — следующий шаг.

### Что дальше

- Показ фото объекта в property-detail / property-card.
- Баги F-2, F-3, комментарии F-13, профиль F-17.
