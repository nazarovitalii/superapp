# 2026-06-18

Карточка объекта: дизайн → слой 1 (фронт) → слой 2 (данные + Project-блок). Всё через
Subagent-Driven Development. README переписан в детальный founder-обзор.

## Что сделали

### Дизайн карточки (умбрелла-спека + план)

- Декомпозировали «карточку объекта» на **4 слоя** (фронт-редизайн / обогащение get_property /
  метрика контакта / layouts). Спека: `docs/superpowers/specs/2026-06-17-property-card-design.md`,
  план слоя 1: `docs/superpowers/plans/2026-06-17-property-card-layer1.md`.

### Слой 1 — фронт-редизайн карточки (задеплоен)

Subagent-Driven: 7 задач, на каждую имплементер + ревьюер, фиксы по находкам, финальный ревью (opus).

- 3 таба Details/Comments/Metrics (Metrics только владельцу); Metrics-метрики; кнопка избранного
  под фото; no-photo «No Photo» (серый, 1/3 высоты); Tech-блок «Поле:Значение» + композиция Type;
  кнопки действий центрированы вне блоков. Сюит 10973 ✅.
- Ревью поймал: `fixture: any`, `setInterval`-polling в тесте, регрессию Handover/Completion,
  отсутствие CSS у таба Metrics, мёртвый specs-grid SCSS — всё исправлено фикс-субагентами.

### Слой 2a — is_vastu + лента + форма (задеплоен)

- БД: миграция колонки `properties.is_vastu` + `get_feed` возвращает `is_vastu` (обе применены, верифицированы).
- Лента: беды = число сверху, `maid` серым снизу; **vastu в ленте не показываем** (решение).
- Форма: чекбокс «Vastu» только для residential `apartment`/`house`.
- Pre-push поймал баг (is_vastu обязательным в типе → спеки не компилировались) — фикс-субагент
  сделал поле optional, полный сюит 10987 ✅.

### Слой 2b — get_property + карточка (задеплоен)

- Миграция `get_property` (M-2b): `agent.active_listings_count` + `public_location_path` (slider-адрес)
  - `is_vastu` + **`project`** (из `location_developers` по leaf-`location_id`, скаляр-подзапрос LIMIT 1).
    Применена, верифицирована на проде (project{} наполнен для Damac Hills), `database.md` синхронизирован.
- Карточка: **Project-блок** (Name/Building·Cluster·Project/Developer/Completion/Handover),
  «Активных листингов: N» в Agent, slider-адрес в Расположении, «+ vastu» в Bedrooms. Маппинг покрыт
  тестами, ревью Approved, сюит 10988 ✅.

### Документация

- `docs/README.md` переписан в **детальный обзор для основателя**: что это простыми словами,
  что уже работает на проде, поток агента end-to-end, дорожная карта, карта документации.
- `tabs.md`, `TODO.md` (WP-G), `database.md`, спека — обновлены.

## Решения

- **location_developers переделана под leaf-уровень** (в параллельном чате). Project-блок матчится
  по `location_id = property.location_id`, `developer_name` денормализован (JOIN developers не нужен).
  Маппинг: Name←project_group_name; Building/Cluster/Project по `is_building`; Completion из
  project_status (under_construction/planned→Off-Plan, completed→Ready); Handover completed→built_year,
  иначе completion_q+year. Наполнено пока только Damac Hills — по другим районам project=null (норма).
- **Vastu** — только residential apartment/house; в ленте НЕ показываем (число beds + maid серым).
- **Реализацию веду через Subagent-Driven** (пользователь поправил после прямой правки ленты) — даже
  мелкие правки субагентами. Сохранено в память.
- **get_property переписывали один раз** после rework (active_listings/slider/is_vastu/project вместе),
  чтобы не делать двойную работу.

## Где остановились

Слой 2 карточки закрыт полностью и задеплоен. Прод: лента + карточка с Project-блоком, формой vastu,
slider-адресом. README-founder готов. Вся документация обновлена и сохранена.

## Что дальше

- **Слой 3** — реальная метрика «контакт просмотрен» (таблица дедупа по юзеру + запись на клик
  WhatsApp + строка в Metrics). Нужна миграция.
- **Слой 4** — layouts (community_layouts: автокомплит + fuzzy + создание + модерация в Control).
- **Комментарии** — бэкенд таба Comments (F-13e).
- **Настоящий вход Telegram** (M-9).

---

## Сессия 01:36 — правки формы и карточки (FC-1..FC-4)

### Что сделали

- **Разобрали процессный сбой:** по `прочитай /remember` нашли, что ранние просьбы создателя по
  форме («Сделка→Категория», «Фото+Планировка отдельным шагом», «maid/hotel/vastu тремя строками»)
  нигде не были зафиксированы → не выполнены. Записали их в спеку
  `docs/superpowers/specs/2026-06-18-form-and-card-fixes-design.md` + строки FC-1..FC-4 в TODO.
- **Реализовали 4 правки через Subagent-Driven** (имплементер + ревьюер на задачу + финальный кросс-ревью на opus):
  - **FC-1** Карточка «Характеристики»: иконка `tune`→`info`; раскладка **Вариант A** (построчно
    `Label: Value`, левосторонне, без правой колонки; тот же стиль у блока «Проект»).
  - **FC-2** Форма, шаг «Параметры»: maid/hotel apartment/vastu — три строки, галочка слева у метки
    (новый `.check-row`, общий `.toggle-row` не тронут).
  - **FC-3** Форма: Off-Plan заблокирован при `project_status='completed'` у локации (дизейбл чипа +
    гард клика + реконсиляция при смене локации). `BuildingInfo`+`project_status`, БЕЗ миграции.
  - **FC-4** Форма: «Сделка» влита в «Категория»; «Планировка»+«Фото» вынесены в новый шаг
    «Фото и планировка»; все 8 шагов и `_validateStep` перенумерованы.
- **Задеплоили:** сюит 11009 ✅, прод-билд ок, push одним вызовом, TG-summary.

### Решения

- Иконка карточки = `info`; новый порядок шагов: Категория(+Сделка) · Адрес · Параметры · Цена ·
  Состояние · Листинг · **Фото и планировка** · Описание.
- `project_status` пуст (нет строки location_developers) → ограничений на Off-Plan нет.
- Планировка пока чипсами `community_layouts`; автокомплит — Слой 4.
- Все 4 правки клиентские, миграции не нужны (колонка `project_status` уже в БД).

### Где остановились

Задеплоено в прод (remote `d6372edc2`). Известный мелкий нюанс: 12 warning «Unused eslint-disable
directive» в `add-property-page.component.spec.ts` — конфликт конфигов `checkFile` vs полный
`ng lint` (директивы нужны для checkFile, но не для ng lint). Только warning, 0 errors, CI не падает.

### Что дальше (на паузе до закрытия формы — теперь закрыта)

- **Слой 3** — метрика «контакт просмотрен» (миграция: таблица дедупа + RPC + строка Metrics).
- **Слой 4** — layouts (автокомплит + fuzzy + модерация в Control).
- **Комментарии** — бэкенд таба Comments (F-13e).
- **M-9** — настоящий вход Telegram.

---

## Сессия 03:00 — форма добавления v3 + бегунок + лента (батч 2026-06-18b)

### Что сделали (8 задач Subagent-Driven + финальный кросс-ревью opus; автономно, по просьбе создателя «деплой пока сплю»)

- **AP-8/B1:** Sale/Rent первой строкой шага 1; чекбоксы maid/hotel/vastu — метки слева, чекбоксы
  колонкой; «Vastu»→«Vastu Compliant»; `layout_id` вернулся в «Параметры» над площадью, только `house`.
- **AP-11/B2:** спальни/санузлы обязательные + серые звёздочки `*` на всех обязательных полях.
- **AP-9/B3:** последний шаг разбит — Описание = шаг 7, Фото = шаг 8.
- **AP-7/AP-10/B4:** фото reorder через CDK DragDrop + «Сделать главным» (первое=главное); строка
  «Floor Plan» до 4 фото (reorder); `photo_type='floor_plan'`, префикс пути `fp_`.
- **AP-3/AP-S2/B5:** бегунок «Что видят коллеги» — кастомные круглые точки по уровням, трек от
  центра первого слова до центра leaf, метка по центру, клик по точке ≥ комьюнити.
- **F-center/B6:** колонки ленты Тип/Beds/Площадь/Цена/иконки/Агент центрированы.
- **AP-2/AP-5/B7:** «Vista» не находила «Golf Vista» — причина: клиент слал `p_limit=8`; фикс
  клиентский (descendant-поиск шлёт 50). Developer-автокомплит (RPC `search_developers`) при пустом
  `location_developers`. Оба — без миграции.
- **C1 (поймал финальный ревью):** `getPhotos` не фильтровал `photo_type` → floor-plan-фото лезли бы
  в галерею карточки. Пофикшено: `getPhotos` отдаёт только `gallery`.

### Решения

- Q1 чекбоксы → вариант B (метки слева, чекбоксы выровнены в колонку). Q2 layout_id → «Параметры»,
  над площадью, только residential `house`. Q3 developer-поле → только когда `buildingInfo()===null`.
- AP-2 и AP-5 оказались клиентскими (RPC `search_developers`/`p_limit` уже есть) → **ни одной миграции**.
- Все правки клиентские; финальный ревью на opus поймал кросс-сим баг (C1), пофикшено до деплоя.

### Где остановились

Задеплоено (remote `6f5ec8a96`, Coolify autodeploy). Сюит 11040 ✅, прод-билд зелёный.
Известные мелочи: lint-warning «unused eslint-disable» в спеке формы (0 errors); бегунок — клик (без
драга), точные пиксельные офсеты могут потребовать визуальной подгонки; «Тип сделки» со звёздочкой,
но всегда валиден (дефолт sale).

### Что дальше (на паузе)

- Слой 3 (метрика «контакт просмотрен», миграция) · Слой 4 (layouts автокомплит) · комментарии
  (F-13e) · M-9 (Telegram-вход). Опционально: серверный скоуп `search_locations` к комьюнити (надёжнее
  клиентского limit=50); показ Floor Plan на карточке объекта.

---

## Сессия 04:30 — AP-2 серверный скоуп уточнения адреса (миграция + клиент)

### Что сделали

- Создатель подтвердил: уточнение адреса должно искать **строго в пределах выбранного узла**
  (Damac Hills → его потомки; Golf Town → только ниже Golf Town). Это требовало серверного фикса.
- **Миграция `applied/2026-06-18-search-in-scope.sql`** (написал → создатель применил в Studio →
  проверил вживую через PostgREST): новая RPC `search_in_scope(p_query, p_within_id, p_limit)` —
  потомок = узел встречается в любом FK-предке (city/community/sub_community/cluster/building).
  Проверка: `search_in_scope('vista', Damac Hills)` → Golf Vista (cluster) + Golf Vista 1/2 (buildings);
  `('golf', Golf Town)` → только 28 потомков Golf Town. `search_locations` НЕ трогали (нулевой риск).
- **B-scope (клиент, Subagent-Driven):** `searchInScope(query, withinId, 50)` в сервисе;
  `onChildSearchInput` скоупит по **последнему узлу `addrPath`**, убран клиентский фильтр по
  community_name. Ревью чистое; service 12/12, component 48/48.
- Задеплоено (remote `6722e5784`), сюит 11043 ✅, билд ок. database.md/tabs.md/TODO синхронизированы.

### Решения

- Вместо переписывания большой `search_locations` (риск клоббера) — отдельная маленькая функция
  `search_in_scope`. Универсальный FK-in-list фильтр — уровень узла знать не нужно.
- AP-2 закрыт правильно (серверный скоуп), заменил временный клиентский обход p_limit=50.

### Дальше (на паузе)

Слой 3 (метрика контактов) · Слой 4 (layouts) · комментарии (F-13e) · M-9 (Telegram-вход).
