# 2026-06-12

## Что сделали

- **Inbox-style UI по всем экранам MrSQM** (коммит 479e05f27, задеплоено):
  - **Лента:** чекбокс множественного выбора на каждой записи — переиспользован
    родной `done-toggle` из инбокса SP (та же анимация галочки); выбранные строки
    подсвечены `--task-c-selected-bg`; над списком плашка «Выбрано: N · Сбросить».
  - **Hover-кнопки** в строке ленты — как task-hover-controls в инбоксе: при
    наведении справа выезжают «закладка» и «открыть карточку»; на таче скрыты
    (остаётся статичная закладка).
  - **Карточка объекта (sidebar):** вместо стрелки поверх фото — шапка как в
    фильтрах (стрелка сворачивания + заголовок «Объект»); цена — заголовок с
    нижней линией primary; секции — карточки на токенах `--task-detail-bg`/`-shadow`.
  - **Фильтры:** каждая группа — карточка в стиле input-item модалки task-detail,
    с иконкой и названием (14px).
  - **Форма добавления:** те же токены/шрифты (14px medium подписи).
- Закоммичены висевшие с 2026-06-11 обновления доков (README/architecture/database/tabs).

## Решения

- **Чекбокс в ленте = множественный выбор** (не «отработано» и не избранное) —
  задел под массовые действия: сравнить, отправить клиенту, создать задачи.
- Hover-кнопки показываются только на устройствах с мышью (`@media (hover: hover)`).
- Везде используются готовые токены SP (`--task-detail-bg`, `--task-c-selected-bg`,
  `--task-shadow`…) — все темы (тёмная/светлая/кастомные) работают автоматически.

## Где остановились

Стили задеплоены, пользователь тестирует. Массовых действий у выбора пока нет.

## Что дальше

- Идеи интеграции планировщика SP в CRM (обсуждены, не реализованы):
  1. «Задача из объекта» — кнопка ➕ создаёт задачу SP со ссылкой на объект.
  2. Проект SP = клиент (мини-CRM по сделкам).
  3. Показы как задачи со временем (планировщик дня, конфликты — уже в SP).
  4. Повторяющиеся задачи для рутины риелтора.
  5. «Создать задачи по выбранным» — массовое действие для новой плашки выбора.
  - Рекомендация: начать с №1 + №5.
- Фидбек пользователя по новым стилям после теста.
