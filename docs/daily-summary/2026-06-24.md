# 2026-06-24

## Что сделали

**Эпик LM — управление статусами своих листингов (LM-2…LM-6) — ПОЛНОСТЬЮ + ЗАДЕПЛОЕН.**
Прошли весь цикл: brainstorming → спека → план → реализация (subagent-driven) → применение DDL под гейтами → финальное opus-ревью → деплой.

**Серверное (применено на прод, supabase_admin, все ROLLBACK-смоуки зелёные):**

- **LM-3:** колонка `rejection_reason` + owner-гейт в `get_property` (видна только владельцу — `CASE WHEN owner_id=auth.uid()`; защита PII конкурентам).
- **Ось истечения:** `CREATE EXTENSION pg_cron` (был в shared_preload_libraries → без рестарта); триггер `set_expires_on_activation` ставит `expires_at=now()+30д` при переходе в active (одно правило, renew/republish наследуют); бэкфилл 13 живых active; cron `expire-listings` (03:00 UTC) active→expired.
- **`renew_property(uuid)`** — expired→active сразу; **`republish_property(uuid,numeric,text)`** — rejected/withdrawn→правка+статус (public→pending_review, network→active), RETURNS статус.
- **`delete_property(uuid)`** — guard archived-only + owner-check + аудит `deleted_listings_audit`; FK 11/12 каскадят БЕЗ ALTER (вкл. realtime `filter_matches`/`user_filter_seen`), единственный NO-ACTION `pdf_generations` — явным DELETE в RPC. + `storage_cleanup_queue` + enqueue-триггер (захват префикса `{id}/`).

**Фронт (закоммичен + задеплоен):** баннер статуса (вариант B) + кнопки по статусу + confirm (Удалить жёсткий / Архив мягкий) через переиспользованный `DialogConfirmComponent` (raw-RU через translate-passthrough) + инлайн-edit→републикация + причина отклонения. Точка непросмотра переселена инлайн к адресу (увод от чекбокса). 12+57 тестов зелёные, prodbuild ✓, opus-ревью READY YES (0 crit/0 imp).

## Решения

- **Confirm:** жёсткий на «Удалить» + мягкий на «Архивировать»; «Продлить» — без диалога.
- **«Редактировать» (rejected/withdrawn):** инлайн-правка → авто-републикация (статус по visibility, через RPC, возвращающую истину).
- **Макет действий:** вариант B (баннер статуса возглавляет кнопки) — выбран мной для 30k как корректность ментальной модели (статус-зависимые кнопки требуют видимого статуса рядом).
- **Де-костыливание (после нажима создателя):** durable-чистка Storage (не клиентский remove), owner-гейт rejection_reason, RPC-возврат статуса (не дубль логики на клиенте), авто-истечение (статус `expired` никто не ставил!), аудит удалений.
- **delete без ALTER FK** (FK-аудит: 11/12 уже cascade) → zero cross-repo с realtime.
- **Storage-дренер физ.удаления — ОТЛОЖЕН** (решение «очередь сейчас»): Vault пуст + `net.http_delete` без body → нужен service-key в Vault (DB-дренер) ИЛИ внешний воркер realtime. Очередь захватывает префиксы durable — ничего не теряется.
- **Точка непросмотра:** инлайн перед адресом (выбор создателя из 2 макетов).
- Деплой: lint+prodbuild гейты вручную → push `--no-verify` (флейк pre-push, не наш).

## Где остановились

Эпик LM полностью применён на прод + запушен (`df046bfb4..b6b5807b0`, 16 коммитов). Coolify собирает. TG ✓. Переписка экспортирована. Доки-коммит (переписка/резюме) — локально, НЕ запушен (CI cancel-in-progress отменил бы LM-билд) → уедет со следующим деплоем.

## Что дальше

- **Визуальная проверка на проде** после Coolify (~10 мин): баннер статуса по 6 статусам на своём объекте; confirm Удалить/Архив; продление expired; переопубликация rejected/withdrawn; точка непросмотра инлайн.
- **Storage-дренер физ.удаления** (follow-up): выбрать A (service-key в Vault → pg_cron+pg_net поштучно) или B (внешний воркер realtime дренит `storage_cleanup_queue`). До тех пор удалённые объекты чисты в БД, файлы остаются в бакете (записаны в очередь).
- **Доки-досинхрон** (необязательно): README/tabs/tests — LM задокументирован в спеке/плане; cells `[ ]` в TODO LM-таблице (шапка секции уже помечена ✅).
- 4 Minor из ревью (в `.superpowers/sdd/lm-final-review.md`) — не блокируют.

---

## Сессия 15:42

## Что сделали
- **Ревью спеки storage-дренера realtime** (вариант B): сверил с контрактом, дал 3 правки (POLL_MS 5000→30000, смоук гранта service_role, граница комментариев). realtime внёс всё → дал финальный апрув. Зафиксировал факт: бакет `property_photos` уже в проде с данными (TODO/бриф врали «не создан») → задача K = подключить загрузку, не создавать бакет.
- **F-13 (комментарии) — спека + план** (brainstorm пройден, оба коммита). Реализация отложена по просьбе создателя (сначала карточка). Миграция на гейте «да».
- **CD-1/2/3 (карточка) — реализовано и задеплоено** (`e85728c5c`+`4ec8ba8a3`): owner-панель статуса+кнопок перенесена наверх (выше цены) и переоформлена через ui-ux-pro-max (единый блок, шапка «Активно до 20 июля 2026», причина под шапкой, «Удалить» красным справа); «В избранное» скрыто у своих объектов (в карточке `@if(!isOwner())`, в ленте `isOwnItem` в property-card). Тесты 27+16+57 ✅, prodWeb ✅.

## Решения
- **F-13:** треды 1 уровень (FB); Private = плоские личные заметки (без ответов); видимость «All» = виден объект (RPC+RLS-backstop, helper `_can_see_property` 1:1 с `get_property`, заменяемый на SC-10 `can_user_see_property()`); только текст; каскад-удаление уже есть (FK CASCADE). `is_mine` отдаёт сервер. `properties.comments_count` НИКЕМ не поддерживался (все 22 объекта=0) → новый триггер `trg_property_comments_count` (публичные неудалённые); существующий `trg_sync_context_comments` = другой счётчик (user_context, authored).
- **CD:** избранное убирать только у своих (own-only). Срок годности в шапке активного — ДА («Активно до 20 июля 2026»); `get_property` уже отдаёт `expires_at` → миграции не нужно, только добавил в тип `PropertyDetail`.
- **Деплой:** pre-push дал 4 флейк-падения (полный сьют напрямую — чисто) → пуш `--no-verify` (документированная практика).

## Где остановились
CD задеплоено (origin `4ec8ba8a3`, Coolify собирает), TG-summary отправлен. F-13 спека+план готовы, исполнение не начато.

## Что дальше
- Визуальная проверка CD на проде: owner-панель по статусам (Активно до …/Отклонён+причина/Истёк/Продан/Снят), «Удалить» справа красным; избранное отсутствует у своих (карточка+лента).
- F-13: исполнение плана (SDD) — первый шаг миграция БД (нужно «да»).
- Задача K (фото): бакет существует — подключить браузерную загрузку.
