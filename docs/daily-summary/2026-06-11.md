# Daily Summary — 2026-06-11 (superapp / MrSQM)

Большой день: развернули прод, настроили безопасный CI/CD, реализовали auth, ленту на
реальных данных, форму добавления, профиль и избранное; попутно нашли и починили 3 серверных
бага в общей БД.

## 🚀 Инфраструктура и деплой

- **Инцидент:** деплой в Coolify собирал Angular **на прод-VPS** → нехватка памяти →
  завис весь сервер вместе с общей Supabase (БД/админка/парсеры). Ребут восстановил.
- **Исправление архитектуры:** сборка перенесена на **GitHub Actions → GHCR**, Coolify только
  тянет готовый образ (`ghcr.io/nazarovitalii/superapp-web:latest`). VPS в сборке не участвует.
  - workflow `.github/workflows/build-web-image.yml`
- **Прод запущен:** `https://sapp.mrsqm.com` (HTTPS валиден, SPA-роуты ок).
- **Автодеплой:** workflow дёргает Coolify-вебхук после сборки (секреты `COOLIFY_WEBHOOK_URL`,
  `COOLIFY_TOKEN`). Проверено: push → через ~7 мин прод обновился сам.

## 🔐 Auth (M-3)

- `MrsqmAuthService` (Supabase email+пароль, dev), `mrsqmAuthGuard` на всех `mrsqm/*`,
  `LoginPage`, «Выйти» в nav.
- **Важно:** в схеме `users` паролей нет — реальный вход **через Telegram** (M-9, не сделан).
  Текущий парольный вход — **временный dev-костыль**: 9 активным юзерам заведены записи в
  `auth.users`. **Удалить перед продом** (M-3-dev).

## 🏠 Лента (P-2, API-1)

- Подключён **реальный `get_feed`** (фильтры→params, город из `user_context`, пагинация по
  `count_total`). Моки удалены. Пустой результат = empty-state, ошибка = error-state.
- Фиксы отображения: «Тип» (резолв `unit_type_id`→label), «Комьюнити» (добавлено поле в
  `get_feed`).

## ➕ Добавить объект (P-5)

- 5-шаговая форма → прямой `INSERT в properties` под RLS (RPC `publish_property` нет).
  Справочники `get_filter_options`, локация `search_locations`.
- Статус по visibility: network→`active`, public→`pending_review` (модерации в БД нет).
- Фото — отложено (P-5b, нужен Storage-bucket).

## 👤 Профиль (P-6)

- Вкладки **Обзор / Объекты / Активность**. Данные из `user_context` + `users`.
  Мои объекты — прямой запрос к `properties` (со статусом). Реф-код, выход.
- Правка контактов невозможна (нет self-UPDATE RLS на `users` — API-8).

## ⭐ Избранное (P-7)

- **В ленте** (по решению владельца, не отдельный экран): закладка на карточке,
  toggle `save_property`, состояние из `saved_properties`. Экран `/saved` и nav-пункт убраны.

## 🐞 Починенные серверные баги (миграции в `docs/migrations/applied/`)

1. **`activate_user()`** — лезла к `NEW.user_id`, у `properties` поле `owner_id` → INSERT
   объекта падал (никто не мог добавить объект). Ветка по `TG_TABLE_NAME`.
2. **`get_feed()`** — добавлено `community_name` (JOIN на родителя-комьюнити).
3. **`get_agent_listings()`** — был сломан (`>100 args`, ошибка 54023). Разбит на два jsonb.

## ⚠️ Открытые серверные задачи (не чинены)

- **API-8:** на `users` нет self-UPDATE RLS → агент не может править контакты.
- **API-5:** на `properties` нет DELETE-RLS → нельзя удалить объект с клиента.
- **M-9:** настоящий Telegram-вход (`initData` + серверная проверка подписи).
- **P-5b:** загрузка фото (Storage-bucket `property_photos` + RLS).

## 🧪 Качество

- Все правки прошли `checkFile` (prettier+lint) и `tsc`. Unit-тесты MrSQM: **22 зелёных**
  (auth, property-create, feed, profile, saved-properties).
- Боевые проверки RPC/INSERT под JWT (тестовые записи подчищены).

## 📦 Состояние продукта

Замкнут сквозной поток: **вход → добавить объект → объект в БД → лента (реальная) →
избранное → профиль с «Мои объекты»**. Лента наполняется людьми через форму добавления.

## 🔭 Дальше

P-3 (карточка объекта `get_property`), P-8 (сеть), либо серверные политики
(`users_update_own`, DELETE на properties) и M-9 (Telegram-auth).
